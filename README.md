# CardFlow AI

AI 驱动的模块化界面生成系统

## 项目简介

CardFlow AI 是一个创新的智能界面生成系统，用户只需通过自然语言输入需求，AI 会一次性生成完整的执行计划，包括：

- 需要展示哪些模块（功能卡片）
- 每个模块的优先级、初始状态、展示样式
- 每个模块需要调用的 API 及参数
- 用户交互时的 API 映射规则
- 页面的整体布局方式和主题风格

## 核心特性

✅ **AI 完全控制** - AI 不仅决定展示什么内容，还决定如何展示（样式、布局、主题）

✅ **一次性决策** - AI 只在开始时工作，后续交互基于预配置规则，响应快速

✅ **智能样式决策** - 根据用户语言表达自动选择最合适的展示方式

✅ **完全 Mock 支持** - 包括 AI Mock 和 API Mock，无需真实服务即可开发

✅ **模块化设计** - 高度可复用的组件和适配器架构

✅ **TypeScript 严格模式** - 完整的类型安全保障

## 技术栈

**前端：**
- React 18 + TypeScript
- Vite（构建工具）
- Tailwind CSS（样式）
- Zustand（状态管理）

**后端：**
- Node.js + Express + TypeScript
- Cerebras AI（真实 AI，已提供 API）
- Mock 数据（完整的模拟系统）

## 快速开始

### 1. 安装依赖

```bash
yarn install
```

### 2. 配置环境变量

复制 `.env.example` 到 `.env`（已自动配置为 Mock 模式）

### 3. 启动服务

```bash
# 同时启动前端和后端
yarn dev

# 或分别启动
yarn dev:client  # 前端 http://localhost:5173
yarn dev:server  # 后端 http://localhost:3000
```

### 4. 访问应用

打开浏览器访问：http://localhost:5173

## 使用示例

在输入框中输入自然语言需求，例如：

- "订明天北京到上海的机票"
- "快速看一下上海的酒店"
- "查询上海的天气"
- "订后天广州到深圳的机票，顺便看看酒店和天气"

AI 会自动：
1. 分析需求
2. 选择合适的模块
3. 提取参数
4. 决定展示样式
5. 生成完整界面

## 项目结构

```
cardflow-ai/
├── src/                # 前端代码
│   ├── components/     # 通用组件
│   ├── layouts/        # 布局模板
│   ├── modules/        # 模块渲染
│   ├── pages/          # 页面
│   ├── store/          # 状态管理
│   ├── styles/         # 样式系统
│   └── utils/          # 工具函数
│
├── server/             # 后端代码
│   ├── ai/            # AI 相关
│   ├── config/        # 配置
│   ├── modules/       # 模块定义
│   ├── mock-data/     # Mock 数据
│   ├── orchestrator/  # 协调器
│   ├── routes/        # 路由
│   └── middleware/    # 中间件
│
└── shared/            # 前后端共享代码
    ├── types/         # 类型定义
    └── constants/     # 常量定义
```

## 模式切换

### Mock 模式（默认）

适合开发和演示，不需要真实 AI 和数据库：

```env
MODE=mock
AI_MOCK=true
API_MOCK=true
```

### 真实模式

接入真实 Cerebras AI：

```env
MODE=real
AI_MOCK=false
API_MOCK=true  # API 仍可用 Mock
```

### 混合模式

AI 使用真实，API 使用 Mock：

```env
MODE=hybrid
AI_MOCK=false
API_MOCK=true
```

## 当前功能模块

### 🎴 横向滑动列表卡片
**布局特点**：多个选项横向排列，当前项（70%宽度）显示完整信息，待看项（30%宽度）显示简略信息，左右滑动切换

#### ✈️ 航班搜索（Flight）
- 搜索航班
- 查看详情
- 预订功能
- 当前项：完整航班信息（航空公司logo、航班号、时间、价格、舱位）
- 待看项：航空公司名称、价格

#### 🛍️ 商品搜索（Shopping）
- 搜索商品（Amazon Japan, Yahoo Japan, Rakuten）
- 价格对比
- 购买链接
- 当前项：完整商品信息（图片、名称、价格、评分、描述）
- 待看项：商品缩略图、名称、价格

#### 🍽️ 餐厅搜索（Yelp）
- 搜索餐厅、咖啡馆
- 评分和菜系展示
- 营业时间
- 当前项：完整餐厅信息（图片、名称、评分、地址、营业时间）
- 待看项：餐厅名称、评分

#### 🎬 视频搜索（Videos）
- 视频搜索
- 在线播放
- 当前项：完整视频信息（缩略图、标题、播放次数、时长）
- 待看项：视频缩略图、标题

#### 🖼️ 图片搜索（Images）
- 图片搜索
- 查看大图
- 当前项：完整大图、来源、尺寸信息
- 待看项：图片缩略图

### 📰 信息展示卡片
**布局特点**：信息片段横向滑动

#### 🔍 信息搜索（InfoCard）
- 在线搜索当前信息、新闻
- 实时数据（天气、股票、汇率）
- 事实和知识查询
- 当前项：完整标题、摘要、来源、发布时间
- 待看项：简略标题、来源

### 🎛️ 交互操作卡片
**布局特点**：操作选项或对话横向滑动

#### 💬 消息对话（LineGeneralAgent）
- Line、WhatsApp 等消息应用
- 与朋友/他人聊天
- 询问和确认信息
- 当前项：完整聊天界面（联系人、消息输入框、历史记录）
- 待看项：联系人头像、名称

#### 🔧 应用控制（GeneralAgent）
- Gmail 邮件操作
- Google Maps 导航
- Google Calendar 日历管理
- 26个预定义工作流程
- 当前项：完整操作界面（应用图标、描述、执行按钮）
- 待看项：应用图标、名称

#### ⚙️ 工作流程（OrchestrationAgent）
- 多步骤工作流程
- 结合多个来源数据
- 访问用户信息
- 应用程序控制
- 当前项：完整步骤列表、当前进度、执行按钮
- 待看项：工作流名称、进度

### 🗺️ 横向滑动地图卡片
**布局特点**：地图和地点信息横向滑动

#### 📍 会面地点（MeetingView）
- 基于双方位置推荐会面地点
- 交互式地图
- 距离和交通方式
- 导航功能
- 当前项：完整地图、地点名称、地址、距离、交通方式
- 待看项：地图缩略图、地点名称、距离

## 样式决策示例

| 用户输入 | AI 决策 |
|---------|--------|
| "快速看一下" | compact-list + compact 密度 + flat 样式 |
| "详细对比" | list-detail + comfortable 密度 + elevated 样式 |
| "界面简洁点" | flat 样式 + 灰色系 |
| "重要信息" | elevated 样式 + spacious 密度 + 默认展开 |

## 扩展开发

### 添加新模块

1. 在 `server/modules/registry/` 创建模块定义 JSON
2. 在 `server/mock-data/` 添加 Mock 数据
3. 在 `src/layouts/` 实现渲染逻辑（可复用现有布局）
4. 更新 `all-modules.json` 添加模块摘要

### 添加新布局

1. 在 `src/layouts/` 创建新布局组件
2. 在 `LayoutRenderer.tsx` 注册新布局
3. 更新模块定义支持新布局

## 开发建议

- 使用 TypeScript 严格模式，确保类型安全
- 遵循现有的代码结构和命名规范
- 组件复用优先，通过适配器处理差异
- Mock 数据要有代表性，便于测试

## 文档

- [完整架构设计文档](./AI模块化架构设计文档.md)
- [实现计划](./IMPLEMENTATION_PLAN.md)

## License

MIT

---

**CardFlow AI** - 让 AI 智能生成界面，而不仅仅是内容

